/*import storage from "redux-persist/lib/storage"; // import de localStorage pour web
import { persistReducer } from "redux-persist";

const persistConfig = {
	key: "root",
	storage: storage,
	// Ajoutez d'autres options de configuration ici au besoin
};

export default persistConfig;*/
